@page
@model DefaultAdminFirstAccessModel
@{
    ViewData["Title"] = "Primeiro Acesso do Administrador";
}

<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card shadow-sm">
            <div class="card-header">
                <h4><i class="fas fa-shield-alt me-2"></i>@ViewData["Title"]</h4>
            </div>
            <div class="card-body">
                <div class="alert alert-warning" role="alert">
                    <strong>Atenção:</strong> Você está logado com a senha padrão do sistema. Manter esta senha é um risco de segurança significativo.
                </div>
                <p>Recomendamos fortemente que você altere sua senha para uma senha forte e única.</p>

                <div class="d-flex justify-content-between mt-4">
                    <form method="post" asp-page-handler="KeepPassword" class="me-2">
                        <button type="submit" class="btn btn-secondary">
                            <i class="fas fa-times me-1"></i>Manter Senha e Continuar
                        </button>
                    </form>

                    <a asp-page="./ChangePassword" class="btn btn-primary">
                        <i class="fas fa-key me-1"></i>Alterar Senha Agora
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>